// eslint-disable-next-line @typescript-eslint/no-empty-function
export default function fullAddress() {
  // Given a brewery, should return the full address as a string, in the format of: 'name, street, city (state)'
  // If any field is not available, it should be omitted
}
