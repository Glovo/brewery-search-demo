// eslint-disable-next-line @typescript-eslint/no-empty-function
export default function filter() {
  // Given an array of breweries and a [key, value], should return a filtered array excluding the items with that [key, value] combination
}
