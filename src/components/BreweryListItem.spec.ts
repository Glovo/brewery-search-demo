describe('BreweryListItem', () => {
  it('should display the brewery name', () => {});

  it('should display the brewery location {country}, {state}, {city} with icon', () => {});

  it('should display the brewery phone with icon if present', () => {});

  it('should display the brewery website URL with icon if present', () => {});
});
